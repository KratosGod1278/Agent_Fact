{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=file:./prod.db npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma db push --skip-generate && npx prisma generate && npx tsx prisma/seed.ts && npm run start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
